# form handling
## state lifting
- - state lifting is a technique in React where you move the state up to the closest common ancestor of the components that need to share that state. This allows multiple components to access and modify the same state, ensuring that they stay in sync.
